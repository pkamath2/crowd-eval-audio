DROP TABLE WORKER_LOG;

CREATE TABLE WORKER_LOG ( 
  	worker_id VARCHAR(100) NOT NULL PRIMARY KEY,
  	create_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO WORKER_LOG(worker_id) VALUES ('Test121') --UTC timestamp
